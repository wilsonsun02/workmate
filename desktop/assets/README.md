Place desktop app icon files here.

- `app.ico`: used by PyInstaller as the exe icon when building.
- Suggested size: include at least 256x256 in the ico bundle.

If `app.ico` is missing, build still succeeds and falls back to default icon.
